/**
 * plugin-sdk-shim.ts
 *
 * Re-exports the OpenClaw plugin SDK types used by this extension.
 * All types come from "openclaw/plugin-sdk/core" — the stable public surface.
 */

export type { OpenClawPluginApi, AnyAgentTool } from "openclaw/plugin-sdk/core";
